name: SoapUI API test

on: 
    push:
        branches: [ "master" ]
    pull_request:
        branches: [ "master" ]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: addnab/docker-run-action@v3
          with:
            image: smartbear/soapuios-testrunner
            options: -v "${{env.github}}/src/test/soapui":/project COMMAND_LINE="'/%project%/AvansTestSuite-soapui-project.xml'" -it
            run: |
                echo $?