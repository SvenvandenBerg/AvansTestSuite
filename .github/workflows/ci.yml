name: SoapUI API test

on: 
    push:
        branches: ["master"]
    pull_request:
        branches: ["master"]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v3
        - uses: addnab/docker-run-action@v3
          with:
            image: smartbear/soapuios-testrunner
            options: -v=/src/test/soapui:/project -i --entrypoint /bin/bash
            run: |
                echo "Project"
                ls /project
                echo "Test"
                ./SoapUI-5.7.0/bin/testrunner.sh /project/avanstestsuite.xml